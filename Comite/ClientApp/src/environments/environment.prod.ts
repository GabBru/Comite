// src/environments/environment.prod.ts
export const environment = {
    production: true, // Indicates if we are in production mode
    apiBaseUrl: 'https://api.monapplication.com', // base URL of your application in PRODUCTION
  };