// src/environments/environment.prod.ts
export const environment = {
    production: false, // Indicates if we are in production mode
    apiBaseUrl: 'http://localhost:5000', // base URL of your application in PRODUCTION
  };